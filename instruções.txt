1. Criar um ambiente virtual:

abre o powershell como admin
digita "python -m venv envGPM"
isso vai criar uma pasta chamada envGPM
navega até a subpasta scripts pelo powershell
digita "./activate"
isso vai ativar o ambiente virtual no powershell e agora da pra instalar coisas so pra esse projeto

2. atualizar o pip para instalar modulos usando python

digita "python -m pip install --upgrade pip"

3. instalar os requirements

4.  